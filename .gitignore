node_modules/
.env
.env.dev
.env.local
.env.plesk
.env.uat
.env.prod
.DS_Store
npm-debug.log
dist/
coverage/

# Angular build cache
.angular/

# TypeScript incremental build info
*.tsbuildinfo

# Tesseract OCR language data cache (downloaded at runtime)
backend/.tessdata/

# MongoDB backup dumps — contain real prod data (PII, hashed passwords), never commit
backups/

# Frontend build artifacts — never commit
frontend-deploy/
